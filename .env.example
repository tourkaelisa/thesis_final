# Αντίγραψέ το σε .env και συμπλήρωσε τιμές:  cp .env.example .env
# Το .env ΔΕΝ μπαίνει στο git.

# JWT signing secret. Φτιάξε τυχαίο (π.χ. python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=

# Email που προάγεται αυτόματα σε admin στο startup.
ADMIN_EMAIL=elisatourka@gmail.com
